package com.group16.model.entity.actors;

/**
 * Enum representing the different animation or behavior states an actor can be in.
 * This is typically used to control sprite animations or logic based on what the actor is currently doing.
 */
public enum State {
    IDLE,
    WALK,
    SLASH
}
