
package com.group16.controller.scene;

public final class GameOverScene {
        /*private void restartGame() throws IOException {
        isGameOver = false;

        this.mapWidth = RandomIslandMapGenerator.WIDTH;
        this.mapHeight = RandomIslandMapGenerator.HEIGHT;

        gameController.restart();

        waveNumber = 1;
    }*/

    /*
    draw
    if (isGameOver) {
            g2.setColor(Color.BLACK);
            g2.fillRect(0, 0, GameConfig.SCREEN_WIDTH, GameConfig.SCREEN_HEIGHT);

            g2.setColor(Color.WHITE);
            g2.setFont(new Font("Arial", Font.BOLD, 30));
            String message = "YOU LOST, PRESS R TO TRY AGAIN";
            int textWidth = g2.getFontMetrics().stringWidth(message);
            g2.drawString(message, (GameConfig.SCREEN_WIDTH - textWidth) / 2, GameConfig.SCREEN_HEIGHT / 2);
            return;
        }
     */
}